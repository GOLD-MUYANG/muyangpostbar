405报错   restful注解不对

npm run serve 卡38%    vue template不是只有一个根标签

vue前端变量用驼峰命名，不然后端接收不到，包括传参，都是直接驼峰命名

500报错   后端代码有问题

400报错  前后端传的参数对应不上

415报错 @RequestBody必须是json数据才能用，不然报错

java.lang.IllegalArgumentException: argument type mismatch
tableId设置了自增，那么实体里要加上注解表明该字段自增





























每次加点儿新东西，发现都得给其他部分加上额外验证，烦不胜烦
分页查询，无论开始还是进行中，都需要加上模糊查询参数

有一些特效知道一对那个有人写好了，但是自己并不知道名字，连搜索都做不到

太烦人了，有些bug简直无关紧要，就是非得碰见了才能知道，要是一开始有人说这样不行，
或者写文档里，说：不要遍历怎么怎么着的，容易多了

图片寻路径的一个bug始终没有解决，不过倒是不影响使用，就是一直红warn